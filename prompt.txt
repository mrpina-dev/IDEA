Edit idea113-blade-03.html. Make the following changes:

1. TABLE EXPANSION FIX: Replace the current expand table CSS approach entirely. When the expand button is clicked, use JavaScript to move the table-container element out of the .page div and append it directly to document.body as a fixed full-screen overlay. Give it position fixed, top 0, left 0, width 100vw, height 100vh, z-index 2000, background rgba(2,10,4,0.98), overflow auto, padding 20px, and a close button in the top-right corner labeled X COLLAPSE TABLE styled as a tb-btn. When collapsed, remove the overlay styles, remove the close button, and re-insert the table-container back into its original position inside Module 02 below the SW measurement tutorial link. This guarantees the table is never clipped by any parent container.

2. DEFAULT 4 POINTS FIX: Audit every scoring branch in updateScoreAndDots() and moduleComplete(). Find every condition that can return true or award points on a completely blank freshly loaded document with no user input. Any such condition must be blocked. Specifically: hexBoreFitCalcOk() must return false until the student has entered a non-empty nominal diameter value AND clicked Calculate. Do not auto-calculate any fit entry on page load or on DOMContentLoaded. Remove any setTimeout or immediate call to calcFitEntry during initialization. Run a mental simulation of updateScoreAndDots() on a blank document - the result must be exactly 0. Fix any other branches that award points without user input.

3. DUAL UNIT HOVER REVEAL: Replace the dual-cell stacked layout in the component table for the thickness and OD columns. Each cell should show only the inch value as a visible input field. The mm value appears as a small dimmed floating tooltip-style label that shows on hover of the cell, positioned below the input with a subtle green border and bg2 background. The mm value updates live as the inch value changes. The inch input remains the only editable field. The mm field is read-only and display-only. Remove the unit-label bars and dual-sep dividers. Row height should drop to a single input line per cell.

4. MASS COLUMN HOVER BEHAVIOR: The mass column toggle button controls which unit is the primary visible input. When in lbs mode, the lbs input is visible and the grams value appears on hover as a dimmed floating label. When in grams mode, the grams input is visible and the lbs value appears on hover. The hover label should match the same style as the thickness and OD hover labels from change 3. Update updateMassCells() and all related rendering to reflect this behavior.

5. ASSEMBLY SCREENSHOT IMAGE SIZE: In the assembly screenshots section in Module 04, make uploaded images fill the full width of their container. Set image height to a minimum of 200px with object-fit cover. Remove the image-grid CSS grid layout for these four zones and replace it with a simple block display where each image fills 100% width of the upload-zone-wrap container. Remove dead space by eliminating any fixed small height constraints on .image-item img for these zones specifically. The images should feel large and prominent, not thumbnail-sized.

6. PACK AND GO SUBMISSION CALLOUT: Add a styled callout block just above the Academic Integrity Declaration at the bottom of the document. Title it FINAL SUBMISSION — PACK AND GO using the amber callout style matching the prework-callout. Content: Before submitting, you must export your SolidWorks assembly using Pack and Go so all referenced files are included. Without this step, your assembly will open with missing parts on any other computer. Steps: Step 1 - In SolidWorks, go to File then Pack and Go. Step 2 - Check Include all referenced components and Save to Zip file. Step 3 - Name the file with your name and upload the zip to Google Classroom. Step 4 - Then click Export HTML in the toolbar above and upload the HTML file to Google Classroom. Both files are required for a complete submission. Include a gold-styled Watch Tutorial button that opens https://youtu.be/pjDnke2rENM in a new tab, matching the same button style used for other tutorial links in the document.

Save the file in place. Do not change any other behavior.
