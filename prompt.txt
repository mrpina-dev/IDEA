Edit idea113-blade-03.html. Make the following changes:

1. AUTO-SAVE STATUS: Make the auto-save status indicator more prominent. Move it to a fixed position in the toolbar right side. When a save occurs, flash it bright green with the text SAVED. During idle, show AUTO-SAVE ON in dim. If localStorage is full, show STORAGE FULL in amber. Animate the transition smoothly.

2. SW MEASUREMENT TUTORIAL: In Module 02, above the component table, add a gold-styled link button labeled Watch: SolidWorks Measurement Tool that opens https://youtu.be/4KrSyDHD6uA?si=YH5w-ntCEHHIYZJJ in a new tab. Place it next to or below the mass override instructions callout.

3. DEFAULT MASS MODE TO POUNDS: Change the initial mass input mode to pounds instead of grams. On first load the toggle button in the mass column header should show lbs and all mass inputs should accept lbs as primary with grams as the conversion display.

4. TOTAL MASS UNIT SYNC: The total assembly mass stat card in Module 02 and the assembly mass validation card in Module 04 should both reflect the current mass input mode. When toggled to lbs, display the running total in lbs with grams conversion. When in grams mode show grams with lbs conversion. The toggle button state drives all mass displays globally.

5. COLLAPSIBLE SECTIONS: Add click-to-collapse functionality to every module. Clicking the module header bar anywhere on it toggles the module body open or closed. Add a small chevron indicator on the right side of each module header that rotates when collapsed. Collapsed state saves to localStorage per module. Rubric panels close when the module collapses. Animate the collapse with a smooth max-height transition.

6. INTERFERENCE DETECTION TUTORIAL: In Module 04 near the interference detection radio buttons add a cyan-styled link button labeled Watch: Interference Detection Tutorial that opens https://youtu.be/NjsG8KW4Kp4?si=LrnXdSdr0y9cMzm3 in a new tab.

7. SCREENSHOT ANIMATION REWORK: Redesign the Win+Shift+S animation strip. Make it much more prominent using a dark panel with a glowing border. Show the full key sequence WIN + SHIFT + S in large Orbitron font with each key in its own styled keycap box with a glow effect. Below the keys animate a dotted selection rectangle being drawn across a mock screen area using CSS stroke-dashoffset animation. After the rectangle completes flash a CAPTURED confirmation. The full key sequence should remain visible for at least 3 seconds before any transition. Loop the animation. Make it eye-catching and unmissable.

8. IMAGE COMPOSITING EFFECTS: When images are uploaded anywhere in the document apply: a 1px green glowing border, a subtle scanline overlay using CSS repeating-linear-gradient at low opacity, a slight vignette using radial-gradient overlay, and corner bracket decorations in green at each corner using CSS pseudo-elements. These effects should be CSS-only and removed in print styles.

9. SCREENSHOT INSTRUCTIONS IN SECTION 2 IMAGE COLUMN: In the per-row image upload cell in the component table add a compact version of the Win+Shift+S animation above the upload button. Show keycaps inline and a single-line animated capture indicator. Small enough to fit in a table cell but still readable.

10. MASS OVERRIDE INSTRUCTIONS UPDATE: Replace the existing mass override callout with the following. Context: SolidWorks calculates part mass from geometry and assigned material density. Two situations require overriding this value: (1) You have physically weighed a completed or prototype part using a scale - this is the most accurate value. (2) You have sliced a 3D printed part in Bambu Studio which shows the actual filament mass including infill percentage walls and supports - this is more accurate than SolidWorks geometry mass for printed parts. Steps using the official SolidWorks procedure: Step 1 - Open your part file in SolidWorks. Step 2 - Go to Tools then Evaluate then Mass Properties. Step 3 - In the Mass Properties dialog click Override Mass Properties. Step 4 - Check the Override mass checkbox. Step 5 - Enter your measured or sliced mass value. SolidWorks uses grams internally so convert if needed: 1 lb = 453.6 g, 1 oz = 28.35 g. Step 6 - Click OK then Close. The overridden value will show in the Mass Properties dialog with an asterisk. Step 7 - Enter this value in the mass column of the component table. Note: Override applies per configuration. If you have multiple configurations select All Configurations unless you have a reason to override only the active one. Style this as an expandable amber callout collapsed by default with a header that reads How to Override Mass Properties - click to expand.

Save the file in place. Do not change any other behavior.
