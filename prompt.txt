Edit entry/index.html. Make two targeted fixes:

FIX 1 - BASE PAYOUT PARAMETER NAME MISMATCH
Find the new-contract-submit click handler. In the apiUrl call that sends action=addContract, change the parameter name from payout to basePayout so it reads:
apiUrl({ action:'addContract', name, basePayout: payout, rateLabel, quantity, notes })

Find the detail-save-btn click handler. In the apiUrl call that sends action=updateContract, change the parameter name from payout to basePayout so it reads:
apiUrl({ action:'updateContract', row:activeContractRow, name, basePayout: payout, rateLabel, quantity, status, notes, contractors })

FIX 2 - CONTRACTOR SEARCH REQUIRES STUDENTS TO BE LOADED
Find the detail-contractor-search input event listener. At the very start of the handler, before filtering allStudents, add:

if (!studentsLoaded) {
  document.getElementById('detail-contractor-results').innerHTML = '<div class="result-empty">Loading students...</div>';
  document.getElementById('detail-contractor-results').style.display = '';
  return;
}

This gives immediate feedback instead of silently showing nothing when students are still loading.

Also find the openContractDetail function. At the start of it, add:
if (!studentsLoaded) initStudentsData();

This ensures students start loading as soon as any contract detail is opened, so they are ready by the time the user starts typing in the contractor search.

Commit with message "fix: basePayout param name, contractor search loading state" and push.
