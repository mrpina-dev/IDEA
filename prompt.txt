Edit idea113-blade-03.html. Make the following changes:

1. RESTORE DUAL UNIT CELLS: Revert the thickness and OD columns in the component table back to the original dual-cell stacked layout with inch input on top and mm input below, separated by the dual-sep divider, with unit-label bars showing in and mm. Remove any hover-reveal tooltip logic added for these columns. The dual-cell should look and behave exactly as it did before the hover-reveal change was made.

2. VERTICAL CENTERING IN COMPONENT TABLE ROWS: Add vertical-align middle to every td in the component table. For cells containing flex children (dual-cell, mass-cell, supplier-wrap, row-img-cell), add align-self center and justify-content center where appropriate so the content sits in the vertical middle of the row regardless of row height. Apply this to all td elements in comp-body rows.

3. COUNTERSINK HEAD TREATMENT: In the bolt clearance table, add Countersink (flat head flush/below) as an option in the head treatment select dropdown. Add it between the existing Counterbore option and the Through clearance option. Update the headOpts array in the JavaScript accordingly.

4. FIX DEFAULT 4 POINTS — FULL AUDIT: The document is still awarding 4 points on a blank load. Do the following: First, add a console.log at the very start of updateScoreAndDots() that logs each module score as it is calculated, so the source of the 4 points can be identified. Then trace through every single scoring branch that could award points without any user input. The most likely culprit is either hexBoreFitCalcOk() returning true due to auto-calculation, or a Module 02 hardware scoring branch where hwComps.length equals zero causing the else branch to never fire and defaulting to full hardware score, or a Module 03 N/A checkbox scoring branch awarding points when checkboxes are unchecked. Specifically fix: (a) If hwComps.length is zero, hardware score must be zero, not full points. Change the condition so hardware points are only awarded when at least one hardware component exists and all have supplier and hwDl confirmed. (b) hexBoreFitCalcOk() must return false if no nominal value has been entered by the user. Check that the fc-nom-in input value is a non-empty string with length greater than zero before returning true. (c) Check every N/A checkbox scoring branch — if the checkbox is unchecked and the field is also empty, score must be zero for that item, not the N/A credit. After all fixes, the score on a completely blank document with default state must be exactly 0 pts. Remove the console.log statements after fixing.

Save the file in place. Do not change any other behavior.
