Edit entry/index.html. Add a "Physical coin given?" checkbox to the Award flow.

CHANGE 1 - HTML
In the type-grid section, after the closing div of the type-grid, add:
<div id="physical-toggle" style="display:none; margin-top:0.75rem;">
  <label style="display:flex; align-items:center; gap:0.75rem; cursor:pointer; min-height:48px; padding:0.6rem 0.85rem; background:var(--bg1); border:1px solid rgba(0,255,65,0.15); border-radius:3px;">
    <input type="checkbox" id="physical-check" checked style="width:22px; height:22px; accent-color:var(--green); cursor:pointer; flex-shrink:0;">
    <div>
      <div style="font-family:'Orbitron',sans-serif; font-size:0.65rem; letter-spacing:0.12em; text-transform:uppercase; color:var(--green);">Physical coin given?</div>
      <div style="font-size:0.58rem; color:var(--dim); margin-top:0.15rem; letter-spacing:0.04em;">Uncheck if you cannot give the coin right now</div>
    </div>
  </label>
</div>

CHANGE 2 - SHOW/HIDE TOGGLE
In selectType(), show #physical-toggle only when type === 'Award'. For all other types hide it and reset the checkbox to checked.

CHANGE 3 - SUBMISSION LOGIC
In the submit handler, when the type is 'Award', check if #physical-check is checked:
- Checked: log as type "Award" (physical coin given, no Bank Balance change)
- Unchecked: log as type "Award - Held" (coin not yet given, Bank Balance goes up)

For all other types, submit as-is with no change.

CHANGE 4 - REASONS FOR AWARD - HELD
In the REASONS object, add:
"Award - Held": same array as Award reasons

In the PRESETS object, add:
"Award - Held": same array as Award presets

CHANGE 5 - RESET
In resetLog(), reset #physical-check to checked and hide #physical-toggle.

Commit with message "feat: physical coin checkbox on Award, Award - Held type support" and push.
