Open /IDEA/idea113-blade-04.html and make the following two changes:

1. GMAIL OPEN IN MAIL FIX - Students use Gmail in Chrome on Windows 10. The mailto: protocol does not open Gmail reliably in this setup. Replace the window.open mailto: calls in both openPQMail() and openWOMail() with direct Gmail compose URLs using this format:
https://mail.google.com/mail/?view=cm&to=RECIPIENT&su=ENCODED_SUBJECT&body=ENCODED_BODY
Use encodeURIComponent() for subject and body. The To field should pre-fill apina@boscotech.edu. Update both functions. Do not change the Copy Email logic.

2. PRINT QUEUE SELF-SERVICE NOTE - In the Print Queue tool panel body, directly above the cards container div, add a clearly visible amber callout box stating that the print queue is for scheduling only when all printers are occupied. If a printer is available, students are expected to slice and run the print themselves. Keep it to two sentences. Style it as an amber bordered notice consistent with other amber callouts in the file.

After all changes, commit with message "fix: Gmail compose links, print queue self-service note" and push to origin.
