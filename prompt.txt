Edit coins/index.html. Make the following changes to the leaderboard:

CHANGE 1 - LIFETIME EARNED CALCULATION
In renderLeaderboard(), for each student row, calculate:
const lifetimeEarned = awarded - fines;
where awarded = parseFloat(p('Awarded') || 0) and fines = parseFloat(p('Fines') || 0) already read from Summary CSV.

CHANGE 2 - SORT DROPDOWN
Find the sort dropdown (select element for leaderboard sorting). Replace its options entirely with:
- Lifetime Earned (High to Low) [value: "lifetime-desc"] — this is the new default
- Lifetime Earned (Low to High) [value: "lifetime-asc"]
- Coin Balance (High to Low) [value: "balance-desc"]
- Coin Balance (Low to High) [value: "balance-asc"]
- Most Awarded [value: "awarded-desc"]
- Most Fined [value: "fined-desc"]
- Most Spent [value: "spent-desc"]
- Name (A - Z) [value: "name-asc"]
- Name (Z - A) [value: "name-desc"]
- Section [value: "section"]

Update the sort logic in renderLeaderboard() to handle all options including the two new lifetime-desc and lifetime-asc cases which sort by lifetimeEarned.

CHANGE 3 - DEFAULT SORT
Change the default sort value from whatever it currently is to "lifetime-desc". Update the localStorage key idea-coins-sort default to "lifetime-desc" as well.

CHANGE 4 - LIFETIME EARNED ON CARDS
In the coin-meta section of each leaderboard card, add Lifetime Earned as the first meta item before the existing items:
- Label: LIFETIME (Orbitron, dim, uppercase)
- Value: fmtNum(lifetimeEarned) with + prefix if positive, in var(--green)

Keep all existing meta items (Net Awards, Net Fines, Net Spent) after it.

CHANGE 5 - COIN BALANCE ON CARDS
Keep Coin Balance displayed on the card as the large central number (existing .coin-balance element) since it still represents the student's actual economic standing. Do not remove or replace it.

Commit with message "feat: lifetime earned sort default, card meta, leaderboard ranking by lifetime earned" and push.
