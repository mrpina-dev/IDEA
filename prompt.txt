Edit coins/index.html. In the renderLeaderboard() function, find the .coin-meta div inside each card. It currently shows two items: Net Awards and Net Fines. Add a third item: Net Spent.

- Label: "Spent"
- Value: the Spent column from Summary CSV, read via pick(row)('Spent')
- Color: var(--amber)
- Format with fmtNum(), prefixed with a minus sign

Apply to all cards including negative balance cards.

Commit with message "feat: add net spent to leaderboard cards" and push.
