Edit entry/index.html. Fix the weekly payout check screen with two changes:

--- FIX 1: GIVE COIN LIST BUG ---
Find the renderPayout function. In the else branch where it builds the raw give and wage lists, add data.payOut to the give list lookup:

Change:
give: data.eligible || data.give || data.giveCoin || data.give_coin || [],
wage: data.debt || data.wageOnly || data.inDebt || data.wage_only || []

To:
give: data.payOut || data.eligible || data.give || data.giveCoin || data.give_coin || [],
wage: data.wageOnly || data.debt || data.inDebt || data.wage_only || []

--- FIX 2: ADD PAYOUT LOGGING FUNCTIONALITY ---

Redesign the renderPayout function to make the Give Coin section actionable.

Track paid students in a local Set: const paidThisSession = new Set();

GIVE COIN SECTION:
- Add a "PAY ALL" button above the Give Coin list (green border, full width, Orbitron):
  Label: "Pay All (N) Students"
  On tap: loops through all give students, logs a Payout transaction for each, marks all as paid.
  Disable and show "All Paid ✓" after all are paid.

- Each student row in Give Coin gets a PAY button on the right side (small, green border, Orbitron 0.52rem, min-height 36px, padding 0 0.75rem):
  On tap:
  1. Look up student wage from allStudents array (match by name). Default to 1 if not found.
  2. Fire background API call: logTransaction with type=Payout, reason=Coin Payout to Student, name=student name, amount=wage
  3. Update local allStudents balance optimistically
  4. Add student name to paidThisSession Set
  5. Re-render that row: replace PAY button with a green "PAID ✓" badge, dim the row slightly
  6. If all give students are now paid, update the PAY ALL button to "All Paid ✓" and disable it

- Student rows show: name on left, balance in gold on right (before the PAY button), section in dim below name

WAGE ONLY SECTION:
- Keep as read-only list
- Each row shows: name, section in dim, balance in red
- No action buttons needed

After PAY ALL or individual PAY calls complete, show a toast with count: "Payout logged for X students"

Commit with message "fix: payout give coin list bug, add payout logging to payout screen" and push.
