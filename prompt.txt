Edit idea113-blade-03.html. Make the following changes:

1. MASS INPUT TOGGLE: In the component table mass column, add a global toggle button in the column header that switches between grams-primary and pounds-primary input modes. When in grams mode: input is in grams, conversion shows lbs. When in lbs mode: input is in lbs, conversion shows grams. The toggle button should sit in the column header and say [ g ] or [ lbs ] depending on current mode. Store the active mode in a variable and update all rows on toggle. getCompData() should always save both values.

2. BOLT TABLE FORMATTING: Redesign the bolted joint clearance log for clarity. Give each column a color-coded header: joint description and bolt size in green, printed clearance hole in cyan, metal clearance hole in teal, head treatment columns in gold, nut treatment in amber. Add a short plain-English sub-label under each header explaining what to enter. Increase row padding. Make the table easier to read for students unfamiliar with fastener terminology.

3. SNUG FIT TOLERANCES: In Module 03, add a new tolerance card between the press fit card and the wall thickness card titled Snug Fit (Printed-to-Printed). Rule text: Snug fits are used where parts must slide together with light resistance but no play - looser than a press fit, tighter than a clearance fit. For ABS printed parts, the bore should be nominal plus 0.008 in / 0.20 mm. Example: a 0.500 in shaft fits into a 0.508 in bore. Add an N/A checkbox and a textarea for documenting each snug fit pair with a 1-sentence minimum. Wire it into scoring and module completion the same way the press fit card works.

4. MERGE WALL THICKNESS AND FEATURE SIZE: Remove the separate Minimum Feature Size card (card 04). Merge its content into the Minimum Wall Thickness card (card 03), renaming it Minimum Wall Thickness and Feature Size (ABS / 3D Printed). Add the feature size inputs and rule text as a second section within that single card, separated by a horizontal rule. One N/A checkbox covers both (if no printed parts, both are N/A). Update scoring to preserve the same combined point value.

5. SHAFT ENGAGEMENT THRU ALL: In the hex shaft engagement card, add a checkbox labeled Thru-All - shaft passes completely through the body to the base tip, just above the bottom set screw. When checked, disable the planned and modeled depth inputs and set the status to Thru-All specified. Wire into scoring so checking Thru-All satisfies the engagement check.

6. MASS OVERRIDE INSTRUCTIONS: In Module 02, above the component table, add a collapsed callout panel (expand on click, collapsed by default) titled How to Override SolidWorks Mass Properties. Content: Step 1 - Right-click the part in the feature tree and select Properties. Step 2 - Check Override Mass Properties. Step 3 - Enter the measured mass in grams (convert: 1 oz = 28.35 g, 1 lb = 453.6 g). Use this for physically weighed parts or when using Bambu Studio sliced mass. For Bambu Studio: open the sliced file, check the mass shown in the print preview, and enter that value. Style it as an amber callout matching the prework-callout style.

7. CENTER OF MASS INSTRUCTIONS: In Module 04, above the center of mass val-card, add a small instruction block that reads: To find CoM offset in SolidWorks: Evaluate menu, Mass Properties, check Show center of mass on screen. Read the X and Y coordinates from the origin. The offset is the straight-line distance: sqrt(X squared + Y squared). Include a gold-styled link button that opens https://youtu.be/Y8riRSCD-xo?si=WSD6cdG7Tk1vPwxV in a new tab, labeled Watch Tutorial.

8. POST-APPROVAL FORMATTING: Redesign the approval-success div that shows after approval. Give it a prominent bordered panel with a green glow, a large checkmark character in Orbitron font, the text DESIGN APPROVED in large gold Orbitron, and the timestamp below in cyan monospace. Make it look like a real gate clearance, not a plain div.

9. SCREENSHOT IMAGE SIZE: In the per-row image upload cells added in the previous edit, make uploaded thumbnails fill the full width of the cell with object-fit: cover and a minimum height of 80px. Remove dead space around images. Same fix for any other image grids in the document.

10. SCREENSHOT INSTRUCTIONS ANIMATION: Wherever screenshot uploads appear in the document, add a small animated instruction strip above the upload zone. Use CSS animation to show three steps in sequence, cycling every 2.5 seconds: Step 1 shows WIN key symbol, Step 2 shows + SHIFT + S appearing next to it, Step 3 shows a cursor drawing a selection rectangle. Style in dim green monospace. Label it How to take a screenshot on Windows.

Save the file in place. Do not change any other behavior.
