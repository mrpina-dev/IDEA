Do two things:

1. In idea113-blade-05.html, find the phone-access note text that references a QR code in class. Change it to: "Type this URL in your phone browser to log matches during competition."

2. Create a new file called idea113-blade-05-qr.html in the repo root. It should be a single self-contained printable page with a large QR code pointing to https://mrpina-dev.github.io/IDEA/idea113-blade-05.html. Load the QR library from https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js. Show the URL in text below the QR code. Style it cleanly for printing - dark text on white, large QR (at least 300x300), centered, with the label "IDEA-Blade A05 - Scan to log match results" above it. No other UI needed.

Commit "fix: remove QR reference, add printable QR page for A05" and push.
