Edit entry/index.html. Find the updateDetailContractors function. Change the condition for showing the contractors section from:

const show = status === 'In Progress' || status === 'Completed';

To:

const show = status !== 'Cancelled';

This makes the contractor section visible for Open, In Progress, and Completed contracts. Only Cancelled contracts hide it.

Commit with message "fix: show contractor section for Open and Completed contracts" and push.
