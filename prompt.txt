Edit entry/index.html. Make the following two fixes:

FIX 1 - CONTRACTOR SEARCH REQUIRES STUDENTS LOADED
Find the btn-contracts click event listener. Near the top of the handler, before the fetchContracts call, add:
if (!studentsLoaded) initStudentsData();

FIX 2 - BASE PAYOUT AND QUANTITY INPUTS BROKEN ON ANDROID
Find these four input elements by ID: new-payout, new-quantity, detail-payout, detail-quantity.
On each one, change type="number" to type="text" and add inputmode="numeric" pattern="[0-9]*".

Find the updateDetailTotal function and replace the parseFloat lines with:
const base = parseFloat((document.getElementById('detail-payout').value || '').replace(/[^0-9.]/g, '')) || 0;
const qty = parseFloat((document.getElementById('detail-quantity').value || '').replace(/[^0-9.]/g, '')) || 1;

Find the detail-save-btn click handler. Before the apiUrl call, add cleanup:
const payout = (document.getElementById('detail-payout').value || '').replace(/[^0-9.]/g, '');
const quantity = (document.getElementById('detail-quantity').value || '').replace(/[^0-9.]/g, '') || '1';
Use these cleaned variables in the apiUrl call instead of reading .value directly.

Find the new-contract-submit click handler. Before the apiUrl call, add cleanup:
const payout = (document.getElementById('new-payout').value || '').replace(/[^0-9.]/g, '');
const quantity = (document.getElementById('new-quantity').value || '').replace(/[^0-9.]/g, '') || '1';
Use these cleaned variables in the apiUrl call instead of reading .value directly.

Commit with message "fix: contractor search students load, Android number input compatibility" and push.
