Edit entry/index.html only. Make these changes exactly. Do not reformat unrelated code.

=== CHANGE 1: fix the window-toggle onclick (renderRolesWindows) ===

FIND:

          onclick="handleToggleWindow(${JSON.stringify(sec)}, ${open ? 'false' : 'true'})">

REPLACE with:

          onclick="handleToggleWindow(${esc(JSON.stringify(sec))}, ${open ? 'false' : 'true'})">

=== CHANGE 2: fix the retire-holder onclick (renderRolesHolders) ===

FIND:

onclick="handleRetireHolder(${hRow}, ${JSON.stringify(name)})">Retire</button>

REPLACE with:

onclick="handleRetireHolder(${hRow}, ${esc(JSON.stringify(name))})">Retire</button>

=== CHANGE 3: fix the loading label so opening does not say "Closing" ===

FIND:

async function handleToggleWindow(section, open) {
  showLoading(open === 'true' ? 'Opening…' : 'Closing…');

REPLACE with:

async function handleToggleWindow(section, open) {
  showLoading((open === true || open === 'true') ? 'Opening…' : 'Closing…');

Commit with message "fix: roles window toggle and retire button onclick quoting" and push.
