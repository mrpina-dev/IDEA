Edit entry/index.html. Remove the old client-side "Economy Rules" feature entirely. It has been fully replaced by the synced "Reasons" screen.

STEPS:
1. Find the home screen button for "Economy Rules" (likely id contains "economy" or "rules", label text "Economy Rules"). Remove that button element.

2. Find the Economy Rules screen(s) - the screen div(s) it navigates to, where reasons/rules were added, edited, and enabled/disabled client-side. Remove the entire screen div(s).

3. Remove all JavaScript associated with the Economy Rules feature: its click handler, render functions, add/edit/enable/disable handlers, and any state variables only it used.

4. Remove any localStorage keys that the Economy Rules feature used to store reasons/rules client-side (keys likely containing "rules" or "economy"). Delete the read/write logic for those keys.

CRITICAL - do NOT touch any of these, which are the NEW synced reasons system that replaces Economy Rules:
- The "Reasons" home button (id="btn-reasons")
- Screens s-reasons and s-reason-edit
- The fetchReasons, refreshReasons, buildReasonsMap, reasonsFor functions
- reasonsData, reasonsMap variables
- The REASONS fallback object
- Any reason-edit / reasons-list / reasons-filter handlers

After removal, verify the app still loads and the Reasons button works. The Reasons screen is now the single place to manage award, fine, purchase, and payout reasons.

Commit with message "refactor: remove redundant client-side Economy Rules, replaced by synced Reasons" and push.
