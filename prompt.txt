Edit coins/index.html. Add a Contracts tab to the existing tab bar.

--- TAB BUTTON ---
Add a fourth tab button labeled "Contracts" to the existing .tab-bar div after the Analytics tab button.

--- DATA FETCHING ---
Add two new CSV-style fetches inside fetchData() using the existing API base URL pattern. These are JSON fetches not CSV:

const CONTRACTS_API = "https://script.google.com/macros/s/AKfycby_p-lI7I5i4Rl1KZKgtWEhePnUmyrC9HBrS485JBWvcwWfpIWC_397P7yrcrmvIql9/exec";

Fetch contracts: CONTRACTS_API + "?action=contracts&_=" + Date.now()
Fetch contract history: CONTRACTS_API + "?action=contractHistory&_=" + Date.now()

Store results in let contractsData = [] and let contractHistoryData = []. Fetch both with cache: 'no-store'. Call renderContracts() after both resolve.

--- TAB PANEL ---
Add a tab panel #tab-contracts. Inside it, three sub-tab buttons: Active | Completed | History. Style sub-tabs similarly to the main tab bar but smaller - Orbitron, 0.58rem, gold underline on active.

SUB-TAB 1: ACTIVE
Shows contracts with status Open or In Progress, sorted: In Progress first then Open, alphabetical within each group.

Each contract is a card (bg1 background, border radius 4px, padding 1.2rem 1.4rem, margin-bottom 0.8rem):
- Top row: Contract name (Orbitron, 0.88rem, white) + status badge right-aligned
  - Open badge: dim border, dim text
  - In Progress badge: gold border, gold text
- Payout row: Total payout in gold Orbitron (large, 1.4rem) + rate label in dim small text if rateLabel is not empty (e.g. "× 3 chairs")
- Contractors row (only if contractors field is not empty): dim label "ASSIGNED TO" + contractor names in white, comma separated
- Notes row (only if notes not empty): dim italic text, 0.72rem

SUB-TAB 2: COMPLETED
Same card layout as Active but for status = Completed. Show dateCompleted in dim text below the name. Sort newest completed first.

SUB-TAB 3: HISTORY
Shows Contract History entries. Each entry is a simpler row:
- Contract name (white) + event type badge (Completion Reset = cyan, Cancelled = red)
- Date in dim text
- Contractors and payout if present
- Notes in dim italic
Sort newest first.

If any sub-tab has no data, show a dim placeholder message.

--- RENDERING ---
Add function renderContracts() that populates all three sub-tabs. Call it whenever contractsData or contractHistoryData updates.

Wire sub-tab buttons to show/hide sub-tab panels.

--- GENERAL ---
Match existing visual style throughout. No external dependencies.

Commit with message "feat: add Contracts tab to coins leaderboard page" and push.
