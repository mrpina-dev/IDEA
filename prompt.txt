Edit entry/index.html. Find every place where document.getElementById('detail-contractor-results').style.display is set to an empty string ''. Replace every instance with 'block'.

Also find where resultsEl.style.display is set to '' inside the detail-contractor-search input event listener and replace those with 'block' as well.

There should be no remaining instances of contractor-results display being set to empty string after this fix.

Commit with message "fix: contractor results display none fallback, set explicit block" and push.
