Edit entry/index.html. Make the following targeted fixes to the Log Transaction screen:

1. SCROLL INTO VIEW
At the end of the selectType() function, after showing reason-section and amount-section, add:
document.getElementById('reason-section').scrollIntoView({ behavior: 'smooth', block: 'start' });

2. VISUAL CONFIRMATION ON TYPE BUTTON TAP
At the start of selectType(), before anything else, add a brief background flash to the tapped button so Mr. Pina can confirm the tap registered:
- Set the button background to rgba(200,255,0,0.2) for 200ms then revert

3. SIMPLIFY SECTION VISIBILITY
Replace all style.display manipulations for reason-section and amount-section with a class-based approach:
- Add CSS: .hidden { display: none !important; }
- Give both sections class="hidden" in the HTML by default
- In selectType(), remove the hidden class from both sections instead of setting style.display
- This avoids any inline style specificity conflicts

4. ACTIVE STATE ON TYPE BUTTONS
In selectType(), after removing active from all buttons, use querySelector to find the correct button by its data type and add active class. Change each button HTML to add data-type="Award" / "Fine" / "Purchase" / "Payout" attributes. Then in selectType(type): document.querySelector('[data-type="'+type+'"]').classList.add('active');

Commit with message "fix: scroll into view after type select, class-based visibility, button data attributes" and push.
