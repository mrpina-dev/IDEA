Edit coins/index.html. The i¢ Guide modal currently builds its Awards / Purchases / Fines content from hardcoded reason arrays. Repoint it to read from the live Reasons sheet via the API so it stays in sync with the entry app.

API: use the same Apps Script base URL already used for contracts on this page (the constant ending in /exec). The endpoint is:
[that base URL] + "?action=getReasons&_=" + Date.now()
It returns a JSON array of objects: { row, type, reason, detail, active }.

STEPS:

1. Add state near the contracts data: let reasonsGuideData = [];

2. Add a function fetchReasonsGuide() that fetches the getReasons endpoint with cache: 'no-store', stores the array in reasonsGuideData, then calls the guide render function. Wrap in try/catch that fails silently. Call fetchReasonsGuide() once on page load (alongside or right after the contracts fetch) and also when the i¢ Guide modal is opened.

3. Find the i¢ Guide rendering logic and the hardcoded reason arrays it uses. Replace the hardcoded data source with reasonsGuideData. Build the guide sections like this:
   - Awards section: reasonsGuideData where type === "Award" AND active is true
   - Purchases section: type === "Purchase" AND active
   - Fines section: type === "Fine" AND active
   - Do NOT show Payout reasons (internal) and do NOT show any reason where active is false (retired reasons are hidden from students)
   - Within each section, sort reasons alphabetically by reason text
   - Each row shows the reason text, and if the detail field is non-empty, show it as the description/subtext using the existing row styling

4. Remove the old "Inactive" section from the guide if one exists - retired reasons simply don't appear now.

5. Preserve all existing modal UI: the gold callout button that opens it, the modal open/close behavior, and any search filter, type filter, sort, and expand/collapse row interactions. Those should now operate over the fetched data instead of the hardcoded arrays.

6. If reasonsGuideData is empty (fetch not yet returned or failed), show a brief "Loading…" state in the guide body rather than an empty modal.

Keep all styling consistent with the current guide. Do not change anything outside the i¢ Guide feature.

Commit with message "feat: i-cent guide reads reasons from synced sheet via API" and push.
