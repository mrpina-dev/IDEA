Edit coins/index.html. Add an info tooltip system to the leaderboard tab that explains the stats shown on each card.

CHANGE 1 - INFO BUTTON
Add a small circular "?" button next to the leaderboard sort dropdown. Place it on the same row as the sort dropdown, right-aligned. Style:
- font-family Orbitron, font-size 0.55rem, font-weight 700
- width 28px, height 28px, border-radius 50%
- background none, border 1px solid rgba(74,122,82,0.4), color var(--dim)
- cursor pointer, flex-shrink 0
- transition color 0.2s, border-color 0.2s
- On hover: color var(--green), border-color rgba(0,255,65,0.5)
- id: stats-info-btn

CHANGE 2 - POPOVER PANEL
Add a popover div with id stats-info-popover positioned absolute below the ? button. Default display none.

Style:
- position absolute, z-index 200
- background var(--bg1), border 1px solid rgba(0,255,65,0.25), border-radius 4px
- padding 1.2rem 1.4rem, width 300px, max-width calc(100vw - 2.2rem)
- box-shadow 0 8px 32px rgba(0,0,0,0.5)

Inside the popover:
- Header row: "LEADERBOARD STATS" in gold Orbitron 0.6rem letter-spacing 0.2em, and an X close button right-aligned (background none, border none, color var(--dim), font-size 1rem, cursor pointer, min 28px tap target)
- Divider line rgba(0,255,65,0.1) 1px margin 0.75rem 0
- Five stat blocks, each with:
  - Stat name in Orbitron 0.58rem uppercase letter-spacing 0.15em margin-bottom 0.25rem
  - Description in Share Tech Mono 0.7rem color var(--dim) line-height 1.6

Stat blocks in order:

Name: LIFETIME EARNED | Color: var(--green)
Description: Awarded coins minus fines. Determines your leaderboard rank. Spending coins does not affect this number.

Name: COIN BALANCE | Color: var(--gold)
Description: Your current economic standing. Awarded minus fines minus spent. Reflects how many coins you effectively own right now.

Name: NET AWARDS | Color: var(--green)
Description: Total coins awarded to you across all time before any deductions.

Name: NET FINES | Color: var(--red)
Description: Total coins fined from you across all time.

Name: NET SPENT | Color: var(--amber)
Description: Total coins spent on purchases across all time.

CHANGE 3 - TOGGLE BEHAVIOR
In the JavaScript, wire the stats-info-btn to toggle the popover visibility. Clicking outside the popover or clicking the X button closes it. Clicking the ? button again also toggles it closed.

Commit with message "feat: add stat info popover to leaderboard" and push.
