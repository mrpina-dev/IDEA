Edit entry/index.html. Fix two issues:

--- FIX 1: OVERLAYS BLOCKING TOUCH EVENTS ---

Find every overlay element in the file - the PIN screen and the loading overlay. For each:

1. In the CSS, add pointer-events:none by default to both overlays.
2. When showing an overlay (adding active class or setting display), also set pointer-events to auto.
3. When hiding an overlay, set all three: opacity to 0, display to none, pointer-events to none. Do not rely on opacity alone to hide - always also set display:none so the element is fully removed from the interaction layer.
4. Audit every setTimeout or transition callback that hides either overlay and make sure display:none is set at the end of the transition, not skipped.

--- FIX 2: TYPE BUTTON ONCLICK HANDLERS ---

Find the four type buttons (Award, Fine, Purchase, Payout). Verify each has a working onclick that calls selectType() with the correct argument. If any use addEventListener instead of onclick, keep them. Verify the selectType() function:
- Removes active class from all type buttons
- Adds active class to the selected button
- Sets selectedType variable
- Populates the reason list
- Shows reason-section and amount-section by setting their style.display to block with !important applied via setAttribute style
- Scrolls the page down slightly so the reason list is visible

--- FIX 3: REASON AND AMOUNT SECTION VISIBILITY ---

Find reason-section and amount-section divs. Make sure no CSS rule is hiding them with higher specificity than the inline style. Add a CSS rule: #reason-section, #amount-section { display: block; } that only applies when they have a data-visible="true" attribute, and set that attribute in selectType() instead of relying solely on style.display.

Commit with message "fix: overlay pointer-events blocking type button interaction" and push.
