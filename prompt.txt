Edit idea113-blade-03.html. Make the following changes:

1. EXPAND TABLE BUTTON LOCATION: Remove the expand table button from the toolbar. Add it directly above the component table in Module 02, styled as an add-row-btn style button, labeled [ EXPAND TABLE ]. When expanded, label changes to [ COLLAPSE TABLE ].

2. EXPAND TABLE BEHAVIOR FIX: Fix the expand table function so the table actually breaks out beyond the page max-width. When expanded, apply these styles to the table-container: width 100vw, position relative, left 50%, transform translateX(-50%), max-width 100vw. This creates a full-viewport-width table that breaks out of the .page max-width constraint cleanly without cropping columns. When collapsed, remove these styles and return to default overflow-x auto behavior.

3. PASTE INTO ROW IMAGE CELLS: Add clipboard paste support to the per-row screenshot column cells. When the user presses Ctrl+V and a row image cell is the last-hovered or last-clicked target, paste the clipboard image into that row's thumbnail slot. Store the image as base64 the same way handleRowImg() does. Also add a compact paste-hint label inside each row image cell below the upload button that reads: Ctrl+V to paste.

4. ACTIVE UPLOAD ZONE INDICATOR: When the user hovers over or clicks any upload zone or row image cell, set it as the active paste target and add a visible highlight border in cyan with a label that reads PASTE TARGET. When the user moves to a different zone, remove the highlight from the previous one and apply it to the new one. The highlight should be a 2px cyan border with a faint cyan background glow and a small PASTE TARGET badge in the top-right corner using absolute positioning. Apply this to all upload zones in Module 04 and all row image cells in Module 02. Store the last-hovered zone ID in a variable so Ctrl+V always goes to the correct target.

5. HIDE WIN+SHIFT+S IN ROW IMAGE CELLS AFTER UPLOAD: When a row image cell receives an image (via file input or paste), hide the compact screenshot hint (row-ss-hint and row-ss-capture elements) inside that cell. If the image is removed, show the hints again. Mirror the same logic already applied to the assembly screenshot upload zones.

6. DEVIATION LOG FORMATTING: In Module 05, apply document-consistent styling to the post-approval deviation log table. The table headers should use the same Orbitron font, green color, letter-spacing, and bg styling as other table headers in the document. The textarea cells should use the same Share Tech Mono font, green text color, bg2 background, and border styling as other textarea inputs. The Add Deviation button should use the same add-row-btn class. The radio option labels should use the same collab-opt styling already used elsewhere in the document. Make the deviation log table visually indistinguishable in style from the continuity deviation table in Module 01.

7. JOINT TYPE DROPDOWN REORGANIZATION: Restructure the joint type dropdown in the fit calculator. Remove the numbered 1-7 prefix from all entries. Group all entries under named optgroup labels: group 1 labeled Printed Parts use separator, containing: Hex shaft through printed part, Bolt clearance through printed part, Bolt clearance through laser cut metal, Circular shaft or boss - press fit printed to printed, Circular shaft or boss - snug fit printed to printed, Circular shaft or boss - clearance fit printed to printed, Non-circular shaft or boss - snug fit offset. Group 2 labeled Printed to Metal, containing the four printed-to-metal types. Group 3 labeled General Offsets, containing the four general offset types. Use HTML optgroup elements for the grouping so they render as visual separators in the dropdown.

8. UNLOCK FIRST FIT ENTRY: Remove the locked behavior from the hex shaft bore entry. It should behave identically to any other entry - it can be renamed, deleted, and its type can be changed. Remove the locked property from the default data, remove the data-locked attribute, remove the locked check in removeFitEntry(), and remove the disabled attribute on the type select. Update addFitEntry() so no entry is ever created with locked status.

9. REMOVE PRESS FIT AND SNUG FIT FROM ZONE B: Remove the Press Fits and Snug Fits dlim-section cards entirely from the Zone B design limits card. Remove the corresponding na-pressfit and na-snug checkboxes, the pressfit-detail and snug-detail textarea fields, and their sent-count indicators. Remove the handleTolNA calls for pressfit and snug from loadData(). Remove the pressfit and snug scoring logic from updateScoreAndDots() and moduleComplete(). The joint fit calculator entries in Zone A are sufficient documentation for this data.

10. NOMINAL DIAMETER INFO BOX: Increase readability of the nom-tooltip. Set font-size to 12px, line-height to 1.7, max-width to 340px, padding to 14px 16px. Give it a stronger border in cyan at full opacity. Add a background of rgba(2,10,4,0.98). Style the strong label at the top in gold Orbitron font. Make the tooltip appear on hover of the entire label row, not just the icon.

11. FIX DEFAULT 4 POINTS BUG: The score is starting at 4 points because hexBoreFitCalcOk() returns true as soon as the locked hex bore entry auto-calculates on page load. Fix this by changing hexBoreFitCalcOk() to return true only if the first fit entry has a non-empty material selection AND has a calculated result. Since the entry is now unlocked (per change 8), require that the user has explicitly interacted with it - check that the fc-nom-in value is non-empty and the result box is active. Do not auto-calculate on load. Remove the setTimeout that calls calcFitEntry on the locked entry at initialization. The entry should show its inputs blank and require the student to enter a nominal value and click Calculate before earning any points.

Save the file in place. Do not change any other behavior.
