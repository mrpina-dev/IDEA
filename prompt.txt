Edit idea113-blade-03.html. Make the following changes:

1. HIDE SCREENSHOT INSTRUCTIONS AFTER UPLOAD: For every image upload zone in the document, the Win+Shift+S animation and clipboard paste tip should be visible only when no image has been uploaded to that slot. Once an image is present, hide the animation and tip and let the image fill that space. If the image is removed, show the instructions again. Apply this behavior to all upload zones including per-row component image cells and all assembly screenshot slots.

2. MASS OVERRIDE CALLOUT FORMATTING: Reformat the How to Override Mass Properties callout for clarity. Use a clean numbered step list with consistent indentation. Each step on its own line with no awkward mid-sentence wrapping. The conversion values (1 lb = 453.6g, 1 oz = 28.35g) should appear on their own dedicated line after step 5, styled as a small reference card with a border, not inline with the sentence. The Context section should use two clean bullet points with no orphaned lines. The Note at the bottom should be visually separated with a top border. Remove all blank triangle artifacts.

3. AUTO-SAVE TOGGLE: Add a toggle button in the toolbar labeled AUTO-SAVE ON that switches to AUTO-SAVE OFF when clicked. When off, the 30-second interval is cleared and the save status shows AUTO-SAVE OFF in amber. Manual save still works. The toggle state saves to localStorage so it persists across sessions.

4. BEFORE OPENING SOLIDWORKS CLEANUP: Reformat the prework-callout section. Each bullet point should be on a clean single line or wrap neatly with hanging indent so the text aligns consistently. Remove any floating characters or artifacts visible in the current render. Ensure em-tags with bold text do not cause layout breaks. Tighten line spacing slightly and add a subtle left border accent to each item for visual structure.

5. HEX SHAFT BORE INTO FIT CALCULATOR: Remove the standalone Hex Shaft Bore tolerance card from Zone A. Instead, when the page loads, automatically add a pre-populated fit calculator entry with joint type set to Hex shaft through printed part and nominal diameter locked at 0.500in / 12.700mm. This entry should be labeled Hex Shaft Bore and appear as the first entry in the calculator. It cannot be deleted, only have its material and printer combo changed. The result updates live as the student changes the material/printer selection. Wire this entry into module completion scoring in place of the old hex bore card.

6. MASS TOTAL REACTIVE UPDATE: Fix updateStats() so it fires on every keystroke in any mass input field and immediately after any unit mode toggle. Also attach the update to the quantity field so changing quantity recalculates the total instantly. The assembly mass stat card, the lbs/grams conversion display, and the Module 04 assembly mass validation card must all update in real time without requiring any other interaction.

7. EXPAND COMPONENT TABLE BUTTON: Add a button in the toolbar or directly above the component table labeled EXPAND TABLE that toggles the table container between its default constrained width and full viewport width. When expanded, the table stretches to fill the screen and all columns are visible without horizontal scrolling. The button label toggles to COLLAPSE TABLE when expanded. The expand state does not need to persist across sessions.

8. SNUG FIT PRINTED TO METAL: Add the following joint types to the fit calculator dropdown: (A) Printed hole receiving metal shaft or pin - snug fit, (B) Printed hole receiving metal shaft or pin - clearance fit, (C) Printed pocket or slot receiving laser cut metal part - snug fit, (D) Printed pocket or slot receiving laser cut metal part - clearance fit. For these types the calculator asks for the nominal metal dimension and outputs the printed bore or pocket dimension to model. Use these tolerances for printed-to-metal: Snug fit: HIPS +0.25mm, ABS +0.25mm, PLA +0.20mm. Clearance fit: HIPS +0.45mm, ABS +0.45mm, PLA +0.40mm. Add a note on these entries that reads: Metal does not shrink. The printed part must accommodate the full nominal metal dimension plus thermal contraction of the printed material.

9. RENAME CALCULATOR ENTRIES: Add a small editable name field at the top of each fit calculator entry. Default name is Joint 1, Joint 2, etc. Clicking the name makes it editable inline. The name shows in the entry header and saves to localStorage with the entry data.

10. ADDITIONAL VAGUE JOINT TYPES: Add the following generalized joint types to the fit calculator dropdown, grouped under a separator labeled General Offsets: (A) 3D printed hole or pocket for any metal part - general clearance, (B) 3D printed external feature or boss for metal pocket - general clearance, (C) 3D printed hole or pocket for another 3D printed part - general clearance, (D) 3D printed external feature or boss for 3D printed pocket - general clearance. These use the same tolerance values as the corresponding clearance fits already defined. Add a note on these entries that reads: Use these for situations where the exact joint type does not match a specific category above. Verify with a test fit before final assembly.

11. NOMINAL DIMENSION TOOLTIPS: Wherever the fit calculator shows a label for nominal diameter or nominal shaft dimension, add a tooltip or inline help text that reads: Nominal means the designed or specified size before tolerances are applied. For hardware, use the listed bolt or shaft diameter from the product spec sheet or McMaster-Carr page. For custom geometry, use the dimension as drawn in SolidWorks before any fit adjustments. For the hex shaft, nominal is always 0.500in regardless of measured variation. Style the help text as a small info icon that reveals the explanation on hover.

Save the file in place. Do not change any other behavior.
