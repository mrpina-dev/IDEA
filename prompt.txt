Edit entry/index.html. Add a full student and section management system.

--- DATA ---
Add at top of script:
let studentsManageData = [];
let sectionsData = [];
let studentsManageLoaded = false;
let sectionsLoaded = false;

Add fetchStudentsManage() that fetches API + "?action=getStudents" with cache no-store, stores in studentsManageData.
Add fetchSectionsData() that fetches API + "?action=getSections" with cache no-store, stores in sectionsData.
Cache both in localStorage with 5 min TTL keys "idea-entry-mgmt-students" and "idea-entry-mgmt-sections".

--- HOME BUTTON ---
Add a sixth home button after Reconcile Balance:
- Border: rgba(136,221,255,0.35) ice
- Icon: ◉
- Label: "Students" in ice (#88DDFF)
- Sub: "Add, edit, and manage students and sections"
- On tap: fetch both students and sections if not loaded, then show s-students screen

--- SCREEN: s-students (Student List) ---
Header: "Students" logo-sub + back to home + "+" add button (green, right side)
Also a "Sections" button next to the add button (small, cyan border, Orbitron, opens s-sections screen)

Screen body:
- Search input (same style as student search) that filters the list in real time by name or section
- Students grouped by section, each section has a header label
- Within each section, students sorted alphabetically
- Each student row (min-height 56px, bg1, border-bottom):
  - Left: student name (white, 0.88rem) + section in dim below
  - Right: balance in gold/red Orbitron + "INACTIVE" badge in red if not active
  - Tap to open s-student-detail
- At bottom, a collapsed "Inactive Students" section with toggle button showing count
- Show a dim placeholder if no students

--- SCREEN: s-student-detail (Edit Student) ---
Header: student name truncated + back to s-students

Fields:
1. Full Name (text input, required)
2. Section (a custom select component - NOT a native select):
   - Shows current section value in a tappable field
   - Tapping opens a scrollable list of existing sections from sectionsData
   - Last item in list: "+ New Section" which opens s-section-new pre-filled to return here after creation
   - Selected section shown with its color dot
3. Wage (text input, inputmode=numeric, pattern=[0-9]*)
4. Notes (textarea, 3 rows)

Action buttons:
- SAVE CHANGES (gold border) - calls updateStudent API with rosterRow, name, section, wage, notes. On name change, show confirm dialog first: "Renaming will update all transaction history. This cannot be undone."
- DEACTIVATE STUDENT (red border, small) - only shown for active students. Confirm dialog. Calls deactivateStudent API.
- REACTIVATE STUDENT (green border, small) - only shown for inactive students. Calls reactivateStudent API.

After any save, refresh studentsManageData and re-render s-students.

--- SCREEN: s-student-new (Add Student) ---
Header: "New Student" + back to s-students

Fields:
1. Full Name (required)
2. Section (same custom select component as detail, with + New Section option)
3. Wage (text input, inputmode=numeric, default value "1")

CREATE STUDENT button (gold border) - calls addStudent API, returns to s-students, shows success toast.

--- SCREEN: s-sections (Section List) ---
Header: "Sections" + back to s-students + "+" add button (green)

Each section row (min-height 56px):
- Left: a colored dot (12px circle using section color) + section name (white)
- Right: student count (how many active students in that section from studentsManageData) + "INACTIVE" badge if section is inactive
- Tap to open s-section-detail

--- SCREEN: s-section-detail (Edit Section) ---
Header: section name + back to s-sections

Fields:
1. Section Name (text input, required)
2. Color (custom color picker component):
   - A row of 8 preset color swatches (24px circles, tappable):
     #00FF41 (green), #C8FF00 (gold), #00F0FF (cyan), #FF8C00 (amber),
     #FF4444 (red), #88DDFF (ice), #FF88CC (pink), #4A7A52 (dim)
   - Below presets: a text input for hex value (shows # prefix, 6 char input)
     inputmode text, auto-uppercases, validates as valid hex on blur
   - A 32px preview swatch showing the current color (square, border-radius 4px)
   - Tapping a preset fills the hex input and updates the preview
   - Typing in hex input updates the preview live

Action buttons:
- SAVE CHANGES (gold border) - calls updateSection API. On name change, confirm dialog: "Renaming will update all students in this section."
- REMOVE SECTION (red border, small) - calls removeSection API. If section has active students, show error toast "Cannot remove section with active students." instead of calling API.

--- SCREEN: s-section-new (Add Section) ---
Header: "New Section" + back button (returns to wherever opened from - s-sections or s-student-detail/new)

Same fields as s-section-detail (name + color picker).
CREATE SECTION button (gold border) - calls addSection API, refreshes sectionsData, returns to previous screen.

--- COLOR PICKER CSS ---
.color-presets { display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 0.75rem; }
.color-swatch {
  width: 28px; height: 28px; border-radius: 50%; cursor: pointer;
  border: 2px solid transparent; transition: border-color 0.15s, transform 0.15s;
  flex-shrink: 0;
}
.color-swatch:hover { transform: scale(1.15); }
.color-swatch.selected { border-color: var(--white); }
.color-preview {
  width: 32px; height: 32px; border-radius: 4px; flex-shrink: 0;
  border: 1px solid rgba(74,122,82,0.4);
}
.color-input-row { display: flex; align-items: center; gap: 0.75rem; }
.color-hex-input {
  flex: 1; min-height: 48px;
  background: rgba(0,0,0,0.4); border: 1px solid rgba(74,122,82,0.35);
  border-radius: 2px; color: var(--white); font-family: 'Share Tech Mono', monospace;
  font-size: 16px; padding: 0.55rem 0.85rem; outline: none;
  letter-spacing: 0.08em; transition: border-color 0.2s;
  text-transform: uppercase;
}
.color-hex-input:focus { border-color: rgba(0,255,65,0.5); }

--- SECTION DOT ---
.section-dot {
  width: 12px; height: 12px; border-radius: 50%; flex-shrink: 0; display: inline-block;
}

--- GENERAL ---
All new screens follow the existing pattern (position fixed, opacity/pointer-events transition, screen-body overflow-y auto).
Wire all back buttons and navigation correctly.
All API calls use apiUrl() helper.
Show loading overlay during non-background API calls.

Commit with message "feat: full student and section management system in entry app" and push.
