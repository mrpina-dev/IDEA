Edit entry/index.html. Make three changes related to weekly wage:

--- CHANGE 1: REPLACE WEEKLY WAGE CONFIRM DIALOG WITH FULL SCREEN ---

Remove the existing btn-wage confirm dialog flow entirely. Instead, tapping the Weekly Wage home button opens a new screen s-wage.

Add screen s-wage:
Header: "Weekly Wage" logo-sub + back to home button

SECTION FILTERS:
A row of toggle buttons below the header (same style as leaderboard filter buttons on the coins page):
All | IDEA-113 | IDEA-208-1 | IDEA-208-2 | IDEA-304 | IDEA-403
Multi-select - tapping a section toggles it on/off. "All" is a special toggle - selecting it deselects all individual sections and vice versa. Active sections highlighted in gold border and text.
On open, default to All selected.

OVERRIDE AMOUNT:
A field group below section filters:
Label: "Override Wage Amount (optional)"
Input: text, inputmode=numeric, pattern=[0-9]*, placeholder "Leave blank to use each student's stored wage"
If filled, all selected students receive this amount instead of their individual stored wage.

PHYSICAL COIN CHECKBOX:
Same style as Award physical checkbox.
Label: "Physical coins given?"
Checked by default.

STUDENT PREVIEW LIST:
Below the checkbox, a scrollable list showing every student who will be paid based on current section selection. Each row shows:
- Student name (white)
- Section in dim text
- Amount they will receive (their stored wage, or override amount if set) in gold Orbitron
Derive this list from allStudents filtered by selected sections and active status.
Update live as sections or override amount change.
Show a count above the list: "Paying X students"

PAY button (gold border, full width, min-height 56px):
Label: "Pay [N] Students"
Disabled if no students in preview.
On tap: call logWeeklyWageFiltered API with:
- sections: pipe-separated list of selected sections (empty string if All)
- overrideAmount: override value or empty string
- physical: 'true' or 'false'
Show loading overlay during the call.
On success: toast "Weekly wage paid to X students", return to home.
On error: toast error.

--- CHANGE 2: WAGE RAISE DETECTION ON PURCHASE SUBMIT ---

In the submit handler, after the optimistic UI (toast shown, screen returned to home, background API fire started), add an interceptor specifically for Purchase + "Raise Weekly Wage":

if (selType === 'Purchase' && selReason === 'Raise Weekly Wage') {
  After showing the success toast and returning to home, show a second modal overlay (use the existing confirm-overlay styled with a number input) with:
  - Title: "UPDATE WEEKLY WAGE"
  - Message: "[student name]'s weekly wage purchase logged. Set their new wage:"
  - A number input pre-filled with current student wage + 1 (read from allStudents)
  - Label below input: "Current wage: X i¢"
  - Two buttons: SKIP and UPDATE WAGE
  
  On UPDATE WAGE:
  - Call updateStudentWage API with name and new wage value
  - Update allStudents locally: find the student and set their wage property
  - Show toast: "Wage updated to X i¢"
  
  On SKIP:
  - Close modal, do nothing else
  
  Use the existing confirm-overlay and confirm-input elements for this. Add a number input inside confirm-box if confirm-input is not sufficient (it should work as a text input with inputmode=numeric).
}

--- CHANGE 3: USE SECTIONS FROM sectionsData ---

In the s-wage section filter buttons, dynamically build the section buttons from sectionsData (active sections only) instead of hardcoding IDEA-113, IDEA-208-1, etc. If sectionsData is not loaded when s-wage opens, fetch it first.

This ensures new sections added via the Students management screen automatically appear in the weekly wage screen.

Commit with message "feat: weekly wage screen with section filters, override amount, wage raise auto-update" and push.
