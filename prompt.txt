Edit entry/index.html. Add transaction editing and deletion in two places: a global Transaction Log screen and per-student transaction history in the student detail screen.

--- DATA ---
Add at top of script:
let txnLogData = [];
let txnLogLoaded = false;
let activeTxnEdit = null;

Add fetchTxnLog() that fetches API + "?action=getTransactions" with cache no-store, parses JSON array, stores in txnLogData sorted newest first.

Add fetchStudentTxns(name) that fetches API + "?action=getTransactions&name=" + encodeURIComponent(name) with cache no-store, returns the parsed array.

--- HOME BUTTON ---
Add a seventh home button after Students:
- Border: rgba(0,255,65,0.35) green
- Icon: ≡
- Label: "Transaction Log" in green
- Sub: "View, edit, and delete transactions"
- On tap: fetch txnLog if not loaded (show loading overlay), then show s-txn-log screen

--- SCREEN: s-txn-log (Global Transaction Log) ---
Header: "Transaction Log" logo-sub + back to home button

Filter bar below header (sticky, same bg as header):
- Name search input (placeholder "Filter by student...")
- Type dropdown (All / Award / Award - Held / Fine / Purchase / Payout)
- Both filter the visible list in real time without re-fetching

Transaction list:
- Each row min-height 64px, bg1, border-bottom 1px solid rgba(0,255,65,0.05)
- Left side: student name (white, 0.85rem) + date below in dim 0.6rem
- Center: type badge (reuse existing type badge styles)
- Right: amount with sign and color (Award/Award-Held/Payout = green +, Fine/Purchase = red -)
- Reason shown below name in dim 0.65rem truncated with ellipsis
- Tap anywhere on row to open s-txn-edit for that transaction

Show "No transactions found" dim message if list is empty after filtering.
Show transaction count in dim text above the list.

A refresh button in the header right side (small, dim border, ↻) that re-fetches and re-renders.

--- SCREEN: s-txn-edit (Edit/Delete Transaction) ---
Header: "Edit Transaction" + back button returning to wherever it was opened from (s-txn-log or s-student-detail)

Show a read-only summary card at top:
- Student name (gold, Orbitron)
- Date and Type badge on same row
- Current Reason in dim text

Two editable fields below:

1. Amount (text input, inputmode=numeric, pattern=[0-9]*, current value pre-filled, label "Amount (i¢)")

2. Reason (scrollable list matching the existing reason-list style, pre-filtered to the transaction type, current reason pre-selected with .sel class):
   - Award reasons for Award and Award-Held types
   - Fine reasons for Fine type
   - Purchase reasons for Purchase type
   - Payout reasons for Payout type
   - Add a search filter input above the reason list (same as reason-search style)

Action buttons:
SAVE CHANGES (gold border, full width) - disabled if nothing changed. On tap:
  - Call updateTransaction API with row, amount, reason
  - Show success toast "Transaction updated"
  - Refresh the transaction list in whichever screen opened this
  - Go back

DELETE TRANSACTION (red border, full width, smaller) - on tap show confirm overlay with:
  - Title: "Delete Transaction"
  - Full transaction details in the message: "[Date] · [Student] · [Type] · [Amount] i¢ · [Reason]"
  - "This cannot be undone and will affect [Student]'s balance."
  - Cancel and DELETE buttons
  On confirm: call deleteTransaction API with row, show success toast "Transaction deleted", refresh list, go back.

--- STUDENT DETAIL TRANSACTION HISTORY ---
In s-student-detail screen, after the action buttons section, add a divider and a "Transaction History" section.

When s-student-detail opens for a student, fire fetchStudentTxns(name) in the background. While loading, show dim "Loading transactions..." text.

On load, render up to 30 most recent transactions for that student in the same row style as the global log (without the student name column since it's implied). Show "No transactions" if empty.

Each row is tappable and opens s-txn-edit for that transaction. After editing/deleting from here, return to s-student-detail and refresh the transaction list.

Show a "View All" button below the list if more than 30 transactions exist, which opens s-txn-log pre-filtered to that student's name.

--- NAVIGATION ---
Track where s-txn-edit was opened from using a variable txnEditOrigin ('s-txn-log' or 's-student-detail'). Back button and post-save navigation uses this to return to the correct screen.

--- GENERAL ---
All new screens follow existing pattern. Wire all navigation correctly.
Show loading overlay only for initial txnLog fetch and delete operations.
All API calls use apiUrl() helper.

Commit with message "feat: global transaction log with edit and delete, student transaction history" and push.
