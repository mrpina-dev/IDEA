Edit coins/index.html only. Make these three changes exactly. Do not reformat unrelated code.

=== CHANGE 1: compute a short label in renderDrawerTxns ===

FIND:

    const amtPrefix = isPos ? '+' : '-';
    const parts = String(t.date).split(' ');

REPLACE with:

    const amtPrefix = isPos ? '+' : '-';
    const shortType = { 'award - held': 'HELD', 'fine - owed': 'OWED' }[typeLower] || t.type;
    const parts = String(t.date).split(' ');

=== CHANGE 2: render the short label with a full-name tooltip ===

FIND:

      '<td><span class="type-badge ' + typeBadgeClass(t.type) + '">' + escHtml(t.type) + '</span></td>' +

REPLACE with:

      '<td><span class="type-badge ' + typeBadgeClass(t.type) + '" title="' + escHtml(t.type) + '">' + escHtml(shortType) + '</span></td>' +

=== CHANGE 3: stop the badge from wrapping ===

FIND:

.drawer-txn-table .type-badge { display: inline-block; white-space: normal; overflow-wrap: anywhere; max-width: 100%; font-size: 0.4rem; letter-spacing: 0.04em; padding: 0.15rem 0.3rem; line-height: 1.15; }

REPLACE with:

.drawer-txn-table .type-badge { display: inline-block; white-space: nowrap; font-size: 0.4rem; letter-spacing: 0.05em; padding: 0.18rem 0.38rem; line-height: 1.15; }

Commit with message "fix: short single-line drawer type badges for held and owed" and push.
